# YVR Water Fountains Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env files to public repositories!

# Supabase Configuration (required for live database functionality)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here

# Admin Access (keep secure!)
ADMIN_PASSWORD=your_secure_admin_password_here

# Auto-deployment Configuration (required for instant deployment when reviews are approved)
PERSONAL_ACCESS_TOKEN=your_github_personal_access_token_here
REPO_NAME=your-username/yvr-water-fountains

# Note: For GitHub Pages deployment, add these as repository secrets:
# 1. Go to GitHub repo Settings > Secrets and variables > Actions
# 2. Add each variable as a "New repository secret":
#    - SUPABASE_URL
#    - SUPABASE_KEY (use the anon key, not the service key)
#    - ADMIN_PASSWORD
#    - PERSONAL_ACCESS_TOKEN (personal access token for triggering deployments)
#    - REPO_NAME (your repository in format 'username/repo-name')
# 3. The deployment workflow will automatically use them
